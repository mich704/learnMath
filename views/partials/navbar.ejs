
<nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark" style="width: 100%;">
  <%loginUser%>
    <div class="container-fluid">
      <a class="navbar-brand" href="/"><img id="LOGO" src="/images/LOGO.png" alt="" height="40" width="100"></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" aria-current="page" href="/branches">Działy</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/branches/tests">Testy</a>
          </li>
          <% if (loginUser) { %>
            <% if (loginUser.role ==='Uczeń') { %>
              <li class="nav-item">
                <a class="nav-link" href="/users/<%= loginUser._id %>/progress">Twoje postępy</a>
              </li>
              <% if(loginUser.challenges.length>0){ %> 
                <li class="nav-item" id="challengesTab">
              
                    <span class="badge badge-pill badge-primary" id="bubble"></span>
                    <a class="nav-link"  href="/users/<%= loginUser._id %>/challenges">Wyzwania </a>
            
                  
                </li>
              <% } %> 
              <li class="nav-item">
                <a class="nav-link" href="/users/<%= loginUser._id %>/challenges/challengeUserRank">Rzuć wyzwanie</a>
              </li>
            <% } %> 
            <% if (loginUser.role ==='Nauczyciel') { %>
              <li class="nav-item">
                <a class="nav-link" href="/users/<%= loginUser._id %>/challenges/challengeUserRank">Ranking uczniów</a>
              </li>
              <% } %> 
             
          <% } %> 
          
        </ul>
        <ul class="navbar-nav ms-auto">
        <% if (!loginUser) { %>
          <li class="nav-item ms-auto">
            <a class="nav-link" type="submit" href="/login">Zaloguj się</a>
          </li>
          <li class="nav-item ms-auto">
            <a class="nav-link" type="submit" href="/register">Zarejestruj się</a>
          </li>
        <% } else{ %>

          <li class="nav-item ms-auto">
              <a class="nav-link" type="submit" href="/users/<%= loginUser._id %>" >
                <img class="avatar" src="/images/profile/profileLogo1.png" width="30" height="30" alt="logo">
                <%= loginUser.username%> 
              </a>
          </li>
          <li class="nav-item ms-auto">
            <a class="nav-link" type="submit" href="/logout">Wyloguj się</a>
          </li>
        <% } %> 
        </ul>
      </div>
      
      
      
    </div>
</nav>

<style>
    img.avatar {
        filter: brightness(150%);
    }
   #bubble{
      float:right;
      margin-bottom:-0.9rem;
      border-radius: 50%;
      background-color: red;
   }
    
</style>